chain:
  bech32_prefix: evmos
  modules:
    - modules
    - messages
    - auth
    - bank
    - consensus
    - gov
    - slashing
    - distribution
    - upgrade
    - actions
    - inflation
    - sp
    - virtualgroup
    - storage
    - permission
node:
  type: remote
  config:
    rpc:
      client_name: juno
      address: https://testnet-lcd.mechain.tech:443
      max_connections: 20
    grpc:
      address: testent-rpc.mechain.tech:9090
      insecure: true
parsing:
  workers: 5
  start_height: 118978
  average_block_time: 5s
  listen_new_blocks: true
  parse_old_blocks: true
  parse_genesis: true
database:
  url: postgresql://postgres:postgres_mechain@localhost:5432/bdjuno?sslmode=disable&search_path=public
  max_open_connections: 1
  max_idle_connections: 1
  partition_size: 100000
  partition_batch: 1000
  ssl_mode_enable: "false"
  ssl_root_cert: ""
  ssl_cert: ""
  ssl_key: ""
logging:
  level: debug
  format: text
actions:
  host: 0.0.0.0
  port: 3000
