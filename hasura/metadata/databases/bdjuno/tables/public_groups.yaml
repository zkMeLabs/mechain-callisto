table:
  name: groups
  schema: public
array_relationships:
  - name: buckets
    using:
      foreign_key_constraint_on:
        column: global_virtual_group_family_id
        table:
          name: buckets
          schema: public
select_permissions:
  - permission:
      allow_aggregations: true
      columns:
        - id
        - group_name
        - owner
        - source_type
        - extra
        - tags
      filter: {}
      limit: 100
    role: anonymous
