table:
  name: storage_group_member
  schema: public
select_permissions:
  - permission:
      columns:
        - id
        - group_id
        - member
        - expiration_time
      filter: {}
      limit: 100
    role: anonymous
object_relationships:
  - name: storage_group
    using:
      foreign_key_constraint_on: group_id
