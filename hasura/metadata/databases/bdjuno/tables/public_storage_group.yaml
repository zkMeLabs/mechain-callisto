table:
  name: storage_group
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns: "*"
      filter: {}
      limit: 100
      allow_aggregations: true
array_relationships:
  - name: group_members
    using:
      foreign_key_constraint_on:
        column: group_id
        table:
          name: storage_group_member
          schema: public
